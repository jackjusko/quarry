# Packets

Each subdirectory is one staged application. Created by the **job-hunt** skill.

Naming: `YYYYMMDD-<short-company>-<short-role>` (slug, lowercase, hyphens).

See [`../config.md`](../config.md) for required files per packet.
